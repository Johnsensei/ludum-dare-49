import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const A = (props) => {

    return(
        <View>
            <Text>Choice A</Text>
        </View>
    )
}

export default A;